remote_theme: "mmistakes/minimal-mistakes"

plugins:
  - jekyll-remote-theme

minimal_mistakes_skin: "default"

defaults:
  - scope:
      path: ""
    values:
      layout: "single"
      toc: true
      toc_label: "INDEX"
      toc_sticky: true
